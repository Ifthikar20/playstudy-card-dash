VITE_API_URL=http://localhost:8000/api

# ===== AI Voice Narration (Mentor Mode) =====
# TTS API keys are now securely stored in the backend for security.
# Configure TTS providers in backend/.env (see backend/.env.example)
# No frontend API keys needed!
